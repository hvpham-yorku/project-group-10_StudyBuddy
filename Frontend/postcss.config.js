/* 
 * Configuration for PostCSS, used to translate Tailwind custom CSS to regular CSS
 * Initially created with `npx tailwindcss init` and then modified
 */

module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
